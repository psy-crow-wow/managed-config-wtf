- lineinfile:
    path: "{{ config_wtf.path }}"
    regexp: "^SET {{ cvar.key }} "
    state: present
    line: 'SET {{ cvar.key }} "{{ cvar.value }}"'
  loop: "{{ wtf_cvars | dict2items }}"
  loop_control:
    loop_var: cvar
